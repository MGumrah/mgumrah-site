import { balimIkonu } from "../ikon";

export const dynamic = "force-static";

/** Android'in açılış ekranı ve yükleme penceresi bu boyutu ister. */
export function GET() {
  return balimIkonu(512);
}
